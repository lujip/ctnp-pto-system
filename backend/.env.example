SECRET_KEY=your-secret-key-here-change-this-to-random-string
MONGO_URI=mongodb://localhost:27017/
DB_NAME=pto_system
DEBUG=False
HOST=0.0.0.0
PORT=5000
# Optional: comma-separated frontend URLs (defaults allow localhost + private LAN IPs)
# CORS_ORIGINS=http://192.168.77.74:5175,http://localhost:5173
# Optional: allow any origin (use only on trusted internal networks)
# CORS_ALLOW_ALL=True
# Optional: max upload size in megabytes (default: 50)
# MAX_UPLOAD_SIZE_MB=50
